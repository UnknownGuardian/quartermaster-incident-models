import { Stage, Event, metronome, normal } from "../../src";
import {BuildService} from "./build-service";

export class VirtualStage extends Stage {
    constructor(protected wrapped: Stage) {
        super();
    }

    // Build

    // Run

    // Cleanup

}